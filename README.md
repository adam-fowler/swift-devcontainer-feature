# Swift Dev Container Feature

A feature for a dev container that installs the Swift using swiftly.

Example using the feature:

```jsonc
{
    "image": "ubuntu:jammy",
    "features": {
        "ghcr.io/adam-fowler/swift-devcontainer-feature/swiftly:0": { }
    }
}
```
